# Supabase Configuration (Production)
# Get these from your Supabase project dashboard: https://app.supabase.com/project/_/settings/api

# Public URL (safe to expose to client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Anonymous key (safe to expose to client, protected by RLS)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Service Role Key (SERVER ONLY - NEVER expose to client!)
# ⚠️ WARNING: This key bypasses RLS. Only use in Server Actions/API routes.
# In Vercel, mark this as "Server-side" when adding environment variable.
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ============================================================================
# DEPLOYMENT INSTRUCTIONS
# ============================================================================
#
# 1. Create a Supabase project at https://supabase.com
# 2. Run the SQL migration: supabase-schema-secure.sql
# 3. Enable Realtime for all tables (Database → Replication)
# 4. Copy Project URL and keys from Settings → API
# 5. In Vercel dashboard:
#    - Add all three environment variables
#    - Mark SUPABASE_SERVICE_ROLE_KEY as "Server-side only"
#    - Set for "Production", "Preview", and "Development" environments
# 6. Deploy!
#
# ============================================================================

# Node Environment (automatically set by Vercel)
# NODE_ENV=production
