{
  "name": "risk-game-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "cd packages/web && npm run dev",
    "build": "cd packages/game-engine && npm run build && cd ../database && npm run build && cd ../web && npm run build",
    "start": "cd packages/web && npm run start",
    "type-check": "cd packages/game-engine && npm run type-check && cd ../database && npm run type-check && cd ../web && npm run type-check",
    "test": "echo 'No tests yet'",
    "clean": "rm -rf packages/*/dist packages/*/.next packages/*/node_modules/.cache node_modules/.cache",
    "fresh": "npm run clean && rm -rf node_modules packages/*/node_modules && npm install"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "prettier": "^3.6.2"
  }
}
